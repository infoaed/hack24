[model]
name = Dashboard
label = {{ this.title }}
hidden = yes
protected = yes

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[children]
model = dash-sub
order_by = -pub_date, title

[pagination]
enabled = yes
per_page = 10
